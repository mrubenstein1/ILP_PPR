---
title: "PPR Acquisition Policy Under Nonstationarity — `r format(Sys.Date(), '%B %d, %Y')`"
params:
  rate_label: "data_derived"     # render a variant with:  quarto render results.qmd -P rate_label:wetland_high
format:
  html:
    toc: true
    toc-depth: 2
    number-sections: false
    fig-cap-location: bottom
    tbl-cap-location: top
    embed-resources: true        # single, portable, production-ready HTML file
    theme: cosmo
  # docx: default                # uncomment to ALSO render Word (no LaTeX needed)
  pdf:
    fig-pos: 'H'                 # PIN figures where they're written (stops them floating away)
    fig-cap-location: bottom
    tbl-cap-location: top
    include-in-header:
      text: |
        \usepackage{float}
        \usepackage{caption}
        \captionsetup{skip=6pt, font=small}
        \usepackage{needspace}   % reserve vertical space so short tables don't split
        % Render the title, section headings, and any other sans-serif element in the
        % roman *body* font so the document reads as one font. Works under pdflatex,
        % lualatex, and xelatex. (The flextable table text is matched separately, via
        % set_flextable_defaults() in the setup chunk.)
        \renewcommand{\sfdefault}{\rmdefault}
        \raggedbottom            % avoid stretching content to fill pinned-figure gaps
    include-before-body:
      text: |
        % Tighten the gap between the title block and the first line of text.
        % Make this MORE negative to tighten further, less negative to loosen.
        \vspace{-2\baselineskip}
execute:
  echo: false
  warning: false
  message: false
  freeze: false
---

```{=html}
<!-- ════════════════════════════════════════════════════════════════════════════
 HOW THIS DOCUMENT WORKS  (architecture, part 2 of 2)

 • The tables, figures, and maps are generated by 10_results_assets.R, which writes
   them into  output_figs/_<rate_label>/ .  This document only EMBEDS them, so:
       1. tweak the model / rerun 10_results_assets.R  → assets refresh
       2. re-render this .qmd                          → document rebuilds


 • ALL titles + captions live in one place: the `caps` list in the setup chunk.
   Edit them there (not in the figures — the images are intentionally text-free).

 • Numbering is automatic. Anything labelled `#| label: fig-*` becomes Figure 1,2,3,4
   in order (maps included); `#| label: tbl-*` becomes Table 1,2. Reorder the blocks
   and the numbers follow. Cross-reference in prose with @fig-overtime or @tbl-gap.

 • Write your commentary in the italic *[Commentary …]* slots before/after each block
   (delete the placeholder text). Add or remove blocks by copying a block and giving
   it a new label + a new entry in `caps`.
════════════════════════════════════════════════════════════════════════════ -->
```

```{r}
#| label: setup
#| include: false

library(readr); library(dplyr); library(tidyr)
library(flextable); library(knitr)

## Table font: match the document body font.
## IMPORTANT: your PDF renders with lualatex, where flextable expands this into
## \fontspec{...} — so it must be a REAL installed font name, not a generic alias
## like "serif" (which fontspec cannot resolve and which breaks the build). With no
## `mainfont` set in the YAML, the lualatex body font is Latin Modern Roman.
## If you ever set a custom `mainfont`, change this string to match it.
set_flextable_defaults(font.family = "Latin Modern Roman")

rate_label <- params$rate_label
OUT_DIR    <- file.path("output_figs", paste0("_", rate_label))

## run metadata (written by 10_results_assets.R) --------------------------------
meta <- tryCatch(
  jsonlite::read_json(file.path(OUT_DIR, "run_metadata.json"), simplifyVector = TRUE),
  error = function(e) list(generated_date = "unknown", rate_label = rate_label, maps_drawn = NA))
gen_date  <- if (!is.null(meta$generated_date)) meta$generated_date else "unknown"
maps_ok   <- file.exists(file.path(OUT_DIR, "foresight_difference.png"))
maps_missing <- !maps_ok   # referenced by the fallback note (avoids '!expr !x' YAML clash)

## formatters (presentation lives here, not in the asset script) ----------------
fmt_pct <- function(x) vapply(x, function(v) {
  if (is.na(v)) return(NA_character_)
  if (v < 1e-4) return("<0.0001%")
  if (v < 1) { s <- formatC(v, format = "f", digits = 4)
    s <- sub("0+$", "", s); s <- sub("\\.$", "", s); return(paste0(s, "%")) }
  paste0(formatC(v, format = "f", digits = 2), "%")
}, character(1))
fmt_pct2  <- function(x) ifelse(x < 1e-4, "<0.0001%", paste0(formatC(x, format = "f", digits = 2), "%"))
fmt_pairs <- function(x) ifelse(abs(x) < 1, "<1", formatC(round(x), format = "d", big.mark = ","))

DELTA <- "\u0394"; DP <- paste0(DELTA, "pairs")

## palette for table text/header (moved here from the asset script) -------------
COL_MODEL     <- c(greedy = "#B0413E", myopic = "#E2A23B", rolling = "#2E7D5B")
COL_MODEL_TXT <- c(greedy = "#B0413E", myopic = "#B8801E")     # darker amber for text
GREEN_HEADER  <- "#2E7D5B"

SCEN_LAB1 <- c(rcp45_wet = "RCP 4.5 wet", rcp45_dry = "RCP 4.5 dry",
               rcp85_wet = "RCP 8.5 wet", rcp85_dry = "RCP 8.5 dry",
               stationary = "Stationary")

## grouped, model-coloured, zebra-striped table (reproduces the old flextable look,
## but rendered natively inline instead of exported to .docx) --------------------
style_grouped_table <- function(df, groups, subs, footer_text = NULL) {
  vcols  <- names(df)[-1]
  rle_g  <- rle(groups)
  top_w  <- c(1, rle_g$lengths)
  top_l  <- c("Scenario", rle_g$values)
  seps   <- c(1, head(1 + cumsum(rle_g$lengths), -1))          # right-edge group separators
  txtcol <- c(COL_MODEL_TXT, rolling = unname(COL_MODEL[["rolling"]]))
  bd     <- officer::fp_border(color = "#dddddd", width = 1)

  ft <- flextable(df)
  ft <- set_header_labels(ft, values = setNames(as.list(c("", subs)), c("Scenario", vcols)))
  ft <- add_header_row(ft, top = TRUE, values = top_l, colwidths = top_w)
  ft <- merge_at(ft, i = 1:2, j = 1, part = "header")
  ft <- bg(ft, part = "header", bg = GREEN_HEADER)
  ft <- color(ft, part = "header", color = "white")
  ft <- bold(ft, part = "header")
  ft <- align(ft, align = "center", part = "all")
  ft <- align(ft, j = 1, align = "left", part = "all")
  ft <- valign(ft, j = 1, valign = "center", part = "header")
  ft <- bold(ft, j = 1, part = "body")
  for (g in unique(groups)) {
    gcols <- vcols[groups == g]; col <- txtcol[[tolower(g)]]
    if (!is.null(col) && length(gcols)) ft <- color(ft, j = gcols, color = col, part = "body")
  }
  ft <- bg(ft, i = seq(2, nrow(df), by = 2), bg = "#f5f5f2", part = "body")
  ft <- border_remove(ft)
  ft <- hline(ft, i = 1, part = "header", border = officer::fp_border(color = "white", width = 1))
  ft <- vline(ft, j = seps, border = bd, part = "header")
  ft <- vline(ft, j = seps, border = bd, part = "body")
  ft <- padding(ft, padding.top = 4, padding.bottom = 4, part = "all")
  if (!is.null(footer_text)) {
    ft <- add_footer_lines(ft, values = footer_text)
    ft <- fontsize(ft, size = 8.5, part = "footer")
    ft <- color(ft, color = "grey40", part = "footer")
  }
  set_table_properties(autofit(ft), layout = "autofit", width = 1)
}

## build the two display tables from the raw CSVs -------------------------------
t1 <- read_csv(file.path(OUT_DIR, "table_over_baseline.csv"), show_col_types = FALSE)
tbl1 <- data.frame(
  Scenario    = unname(SCEN_LAB1[as.character(t1$scenario)]),
  greedy_pct  = fmt_pct2(t1$greedy_pct),  greedy_dp  = fmt_pairs(t1$greedy_dpairs),
  myopic_pct  = fmt_pct2(t1$myopic_pct),  myopic_dp  = fmt_pairs(t1$myopic_dpairs),
  rolling_pct = fmt_pct2(t1$rolling_pct), rolling_dp = fmt_pairs(t1$rolling_dpairs),
  check.names = FALSE, stringsAsFactors = FALSE)
groups1 <- c("Greedy", "Greedy", "Myopic", "Myopic", "Rolling", "Rolling")
subs1   <- c("% over baseline", DP, "% over baseline", DP, "% over baseline", DP)

t2 <- read_csv(file.path(OUT_DIR, "table_gap_vs_rolling.csv"), show_col_types = FALSE)
tbl2 <- data.frame(
  Scenario   = unname(SCEN_LAB1[as.character(t2$scenario)]),
  greedy_gap = fmt_pct(t2$greedy_gap_pct), greedy_dp = fmt_pairs(t2$greedy_dpairs),
  myopic_gap = fmt_pct(t2$myopic_gap_pct), myopic_dp = fmt_pairs(t2$myopic_dpairs),
  check.names = FALSE, stringsAsFactors = FALSE)
groups2 <- c("Greedy", "Greedy", "Myopic", "Myopic")
subs2   <- c("gap %", DP, "gap %", DP)

## ─────────────────────────────────────────────────────────────────────────────
## CAPTIONS + TITLES — the single source of truth. Edit here.
## (Bold lead-in = the title pulled out of each figure; the rest is the caption.)
## ─────────────────────────────────────────────────────────────────────────────
caps <- list(
  tbl_over_baseline =
    "**Value of acquisition policy over do-nothing baseline, by scenario.**",
  tbl_gap_vs_rolling =
    "**Value Added Gap: rolling vs. myopic/greedy, by scenario.**",
  fig_overtime = paste(
    "**Conservation shortfall: rolling \u2212 myopic/greedy.** Discounted duck-pairs",
    "(cumulative to 2100) not conserved as compared to rolling, by climate scenario.",
    "Rolling is the optimal benchmark (gap = 0); MIP gap ~0.01%."),
  fig_decline = paste(
    "**Landscape decline under optimal acquisition schedule, by scenario.** All scenarios",
    "share a common 2020 anchor [4.76m]. Decline is sharpest in dry scenarios."),
  fig_foresight = paste(
    "**Rolling vs. myopic acquisition sets.** EAUs colored by which policy buys them,",
    "under each climate scenario. Purple cells are parcels acquired under both policies;",
    "blue cells are purchased only under the rolling policies; orange cells are purchased",
    "by the myopic policy but avoided by the rolling policy."),
  fig_prevloss = paste(
    "**Per-parcel prevented-loss: rolling \u2212 myopic.** Blue: rolling invests",
    "here and prevents duck-pair loss; red: myopic invests here instead, securing",
    "absolute conservation benefit but relatively less than rolling. grey: no difference",
    "Coloured cells sum to the value-added gap. Colour is capped",
    "symmetrically at the 98th percentile of |\u0394V|.")
)
```

*Rate scenario: `r rate_label` · results generated `r gen_date`.*

```{=html}
<!-- (Title date above = when this document was rendered; the line just above = when
     10_results_assets.R last refreshed the underlying numbers/figures.) -->
```

# Results

## Acquisition value over baseline

This table compares the total benefit secured across the landscape (2020-2100) via three different acquisition policies (rolling, myopic, and greedy) with a "do-nothing" baseline scenario in which no parcels are acquired. All acquisition policies outperform the do-nothing baseline under climate change scenarios; there is no improvement over the do-nothing baseline under stationarity due to conversion risk set to near zero. 

The rolling policy outperforms the myopic and greedy policies under all climate scenarios, consistent with our expectations that an acquisition policy with foresight should outperform a policy with no or limited foresight. The absolute improvement of any acquisition policy, however, is small: at best, the rolling policy secures improvement over baseline of less than 1%. I believe this is due to a low conversion risk: in the landscape as currently described, conversion risk is quite low (median = 0.0000361%), meaning the additional benefit secured through acquisitions (even the optimal/rolling model) is comparatively low. In addition, these results are from the n=5 budget scenario, in which the decision maker can only acquire 5 median-priced parcels per period (~45-65 parcels total), so only 7% of parcels are ever acquired. The value of any acquisition policy over baseline would presumably increase with a more generous budget. 

```{=latex}
\needspace{13\baselineskip}   % keep this whole table on one page (moves to next page if it won't fit)
```

```{r}
#| label: tbl-over-baseline
#| tbl-cap: !expr caps$tbl_over_baseline
style_grouped_table(
  tbl1, groups1, subs1)
```


## Gap: Foresight vs. Myopic/Greedy

This table compares the gap between total benefit secured across the landscape (2020-2100) in the greedy or myopic policy as compared to the rolling policy. The rolling policy is used as the provably near-optimal benchmark (MIP gap 0.01% across all rolling solves); this table presents the gap in cumulative duck pairs conserved under greedy/myopic policies as compared to that near-optimal benchmark. 

As expected, the greedy policy displays a larger gap than the myopic policy under all climate scenarios. Myopic underperforms optimal by approx. %12-%14; greedy underperforms optimal at around ~42-45%. Under stationarity, there is virtually no difference (as we would expect). 

This "value added gap" becomes our metric of greatest interest: because the absolute value of any acquisition policy in this landscape is relatively low, this metric helps demonstrate the value of foresight in acquisition policy. My interpretation: As compared to the best possible acquisition policy, how well does the greedy or myopic policy perform? How much conservation does a suboptimal acquisition policy leave on the table?

```{=latex}
\needspace{13\baselineskip}   % keep this whole table on one page
```

```{r}
#| label: tbl-gap-vs-rolling
#| tbl-cap: !expr caps$tbl_gap_vs_rolling
style_grouped_table(
  tbl2, groups2, subs2,
  footer_text = paste0(
    "gap % = shortfall as % of rolling's value-added.  ", DP,
    " = rolling value-added \u2212 model value-added (discounted duck-pairs)."))
```


## Value added gap over time

This graph shows how the gap between the rolling policy and the myopic/greedy policies grows over time. This is presented as net changes in duck pairs, using the rolling policy as a benchmark (value added gap for rolling = 0). As expected, the cumulative gap in performance between rolling and greedy/myopic grows over time (positive values only); and greedy is substantially worse than myopic (red always > yellow). 

One counter-intuitive observation: the performance between different acquisition policies looks to be more important under wet scenarios than under dry. More ducks are "failed to be conserved" under wet scenarios than under dry; I would have thought the opposite (dry scenarios drive more decline in ducks, so shouldn’t an optimal acquisition schedule produce a larger gap under dry scenarios?).

[I think this might be driven by the fact that land use conversion is the same across wet/dry scenarios (see Q4 below.). And/or: is this just because there are more ducks overall in wet scenarios? if we displayed shortfall as a % of the total duck population, would we see the difference?]


```{r}
#| label: fig-overtime
#| fig-cap: !expr caps$fig_overtime
#| out-width: 100%
knitr::include_graphics(file.path(OUT_DIR, "overtime_gap.png"))
```


{{< pagebreak >}}

## Landscape decline

Here we show the rolling policy under stationarity and 4 climate scenarios, over time. Under all climate scenarios, landscape totals decline; dry scenarios fare worse than wet, whereas emissions scenario is less of a determining driver.  

```{r}
#| label: fig-decline
#| fig-cap: !expr caps$fig_decline
#| out-width: 85%
knitr::include_graphics(file.path(OUT_DIR, "decline.png"))
```

{{< pagebreak >}}

## Acquisitions by policy: rolling vs myopic

This map shows parcels that are acquired under both rolling and myopic acquisition schedules; those that are acquired only under rolling; and those that are acquired only under myopic. Light blue parcels are those that are purchased only under an optimal acquisition policy; the myopic model skips these parcels. The orange parcels are ones that the myopic policy purchased but the rolling policy declined to purchase. Interpretation here is that light blue parcels are where the rolling policy's advantage comes from; orange is where the myopic dedicated funds sub-optimally. 

```{r}
#| label: fig-foresight
#| fig-cap: !expr caps$fig_foresight
#| out-width: 100%
#| eval: !expr maps_ok
knitr::include_graphics(file.path(OUT_DIR, "foresight_difference.png"))
```

```{r}
#| label: no-maps-note
#| eval: !expr maps_missing
#| results: asis
cat("*(Maps not generated — the spatial schedule was absent when",
    "10_results_assets.R last ran. Run an 08 that persists the schedule, rerun the",
    "asset script, then re-render.)*")
```

{{< pagebreak >}}

## Value added gap by policy: rolling vs myopic

This map shows how rolling and myopic acquisition schedules differ in where and how they prevent duck loss. Cells are highlighted where myopic and rolling schedules differ in acquisition decisions. Blue cells show where the rolling acquisition schedule acquired parcels but myopic did not; this represents addition ducks secured through foresight. Red cells show where the myopic schedule acquired parcels that the rolling schedule ignored; this represents a sub-optimal allocation of budget. 

Darker shades of blue are parcels with higher duck density. Darker shades of red indicate parcels where the difference between realized and potential loss is greater.  

Take away: this map show the spatial distribution of how the two models allocated their identical budget differently. The rolling model invested in parcels that maximized benefit by 2100; the myopic invested in parcels that secured real duck pairs (no loss, only gain), but underperformed the rolling benchmark. 

```{r}
#| label: fig-prevloss
#| fig-cap: !expr caps$fig_prevloss
#| out-width: 100%
#| eval: !expr maps_ok
knitr::include_graphics(file.path(OUT_DIR, "prevented_loss_diff.png"))
```

{{< pagebreak >}}

# Updates


## Temporal alignment of benefit data

After the initial model run, I realized there was a problem with the temporal alignment of the benefit data with the landcover data (and the decision model itself). The benefit data is as follows:

- "Obs" = mean 2007–2017 
- "3059" = mean 2030–2059
- "7099" = mean 2070–2099

Originally, I had aligned it as follows:

- "Obs" = mean 2007–2017 -> 2020
- "3059" = mean 2030–2059 -> 2050 
- "7099" = mean 2070–2099 -> 2100

After talking with Owen, I updated the alignment to use window midpoints :

- "Obs" = mean 2007–2017 -> 2012
- "3059" = mean 2030–2059 -> 2045 
- "7099" = mean 2070–2099 -> 285

Notes: The 2020 baseline is produced by interpolating each RCP×GCM combo on its 2012→2045 segment and then averaging across the four combos into one shared period-0 row (preserves the shared-baseline used for the 2020 risk). Decades 2030/2040 interpolate on the 2012→2045 segment; 2050–2080 on the 2045→2085 segment; 2090 and 2100 are held constant at the 2085 value

I re-ran the model with this midpoint temporal alignment of benefit data; saw little difference in model results. My takeaway is that we can reasonably use this midpoint alignment, and importantly that it doesn’t much matter for model results. 

```{=latex}
\needspace{13\baselineskip}   % keep this whole table on one page
```

```{r}
#| label: tbl-align-midpoint
#| tbl-cap: "**Temporal alignment of benefit data: midpoint comparison.**"
align_mid <- data.frame(
  Scenario = c("RCP 4.5 wet", "RCP 4.5 dry", "RCP 8.5 wet", "RCP 8.5 dry", "Stationary"),
  g_pct = c("+0.02%", "0.00%",  "-0.01%", "+0.01%", "\u2248 0"),
  g_dp  = c("+2,600", "-3,742", "-6,610", "-1,135", "-1"),
  m_pct = c("+0.01%", "+0.01%", "0.00%",  "+0.02%", "\u2248 0"),
  m_dp  = c("+305",   "-5,335", "-8,285", "-2,103", "-1"),
  r_pct = c("+0.02%", "+0.01%", "-0.01%", "+0.02%", "\u2248 0"),
  r_dp  = c("+885",   "-8,471", "-11,626","-2,965", "-1"),
  check.names = FALSE, stringsAsFactors = FALSE
)
groups_mid <- c("Greedy", "Greedy", "Myopic", "Myopic", "Rolling", "Rolling")
subs_mid   <- c(paste0(DELTA, " % over baseline"), DP,
                paste0(DELTA, " % over baseline"), DP,
                paste0(DELTA, " % over baseline"), paste0(DELTA, " ", DP))
style_grouped_table(align_mid, groups_mid, subs_mid,
  footer_text = paste0("\u0394 = change vs. the primary benefit alignment (midpoint",
                       " comparison)."))
```


## Temporal alignment: gap vs. rolling


```{=latex}
\needspace{12\baselineskip}   % keep this whole table on one page
```

```{r}
#| label: tbl-align-gap
#| tbl-cap: "**Temporal alignment of benefit data: gap vs. rolling (midpoint comparison).**"
align_gap <- data.frame(
  Scenario = c("RCP 4.5 wet", "RCP 4.5 dry", "RCP 8.5 wet", "RCP 8.5 dry", "Stationary"),
  g_gap = c("-1.00%", "-0.62%", "+0.13%", "-0.44%", "-0.56%"),
  g_dp  = c("-1,714", "-4,731", "-5,015", "-1,829", "0"),
  m_gap = c("+0.22%", "-1.38%", "-0.74%", "-0.34%", "+0.21%"),
  m_dp  = c("+581",   "-3,136", "-3,341", "-861",   "0"),
  check.names = FALSE, stringsAsFactors = FALSE
)
groups_gap <- c("Greedy", "Greedy", "Myopic", "Myopic")
subs_gap   <- c(paste0(DELTA, " gap %"), paste0(DELTA, " ", DP),
                paste0(DELTA, " gap %"), paste0(DELTA, " ", DP))
style_grouped_table(align_gap, groups_gap, subs_gap,
  footer_text = paste0("\u0394 = change vs. the primary benefit alignment (midpoint",
                       " comparison)."))
```


